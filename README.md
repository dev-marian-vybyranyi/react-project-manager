# Project Manager

A modern React application for managing projects and tasks. This application allows users to create projects, add details like descriptions and due dates, and manage specific tasks within each project.

## Features

- ✨ **Create Projects**: Add new projects with titles, descriptions, and due dates.
- 📋 **Manage Tasks**: Add and delete tasks for specific projects.
- 🗂️ **Sidebar Navigation**: Easily switch between projects or start a new one.
- 🔔 **Input Validation**: Built-in modal for error reporting (e.g., empty fields).
- 🎨 **Responsive Design**: Styled with Tailwind CSS for a premium, clean look.

## Technologies Used

- **React**: Core library for building the UI and managing state.
- **Tailwind CSS**: For modern, utility-first styling.
- **Vite**: For fast development and building.

## Getting Started

### Prerequisites

- Node.js installed on your machine.

### Installation

1. Clone the repository or download the source code.
2. Navigate to the project directory:
   ```bash
   cd react-project-manager
   ```
3. Install dependencies:
   ```bash
   npm install
   ```

### Running the Application

To start the development server:

```bash
npm run dev
```

The application will be available at `http://localhost:5173`.

## Deployment

To create a production build:

```bash
npm run build
```

The optimized files will be in the `dist` folder.
